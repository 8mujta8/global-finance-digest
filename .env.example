# Use the Gmail address that will send the digest.
GMAIL_USER=your-gmail-address@gmail.com
# Create this 16-character app password in Google Account > Security > App passwords.
GMAIL_APP_PASSWORD=replace-with-app-password
